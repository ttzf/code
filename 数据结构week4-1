数据
  数据结构+数据内容+数据流
  
  ·数据流
    ·字节流
    ·字符流
  ·数值分析
    ·数值问题用方程式或函数表示
  ·数据结构
    ·非数值问题
  
基本概念和术语
  //数据结构随着程序设计的发展而发展
  //数据结构的发展并未终结
  ·数据
  ·数据元素
  ·数据项
  ·数据结构的两个含义
    ·数据的逻辑结构
    //存在于我们的脑海里，但是当不需要时可以通过简单的存储（物理）结构来实现相应功能
      ·集合
      ·线性结构：一对一，如线性表/栈/队列
      ·树形结构：一对多，如树
      ·图状结构：多对多，如图
      ·D、S、R集合//P.S.形象->逻辑->符号化
    ·数据的存储结构（物理结构）

抽象数据类型（ADT）
  ·可以看做是定义了一组操作的一个抽象模型
  ·一个抽象数据类型性需要包括哪些操作，这一点由设计者根据需要确定
  ·类：数据+方法
  ADT抽象数据类型名{
    数据对象：<>
    数据关系:
    基本操作：

算法与算法分析
  ·算法：有限长操作序列
  ·算法必须满足以下五个重要特性：
    ·有穷性
    ·确定性
    ·可行性
    ·有输入
    ·有输出
    
例子：
  输入n，求解1到n的值
·大O表示法
  ·算法由控制语句和原操作（预定义数据类型的操作）组成
  ·原操作是算法的核心操作
    例：n阶矩阵相乘算法
  ·logn,n,nlogn,n^2,n^3...
  ·O(1)<O(log2 n)<O(n)<O(nlog2 n)<O(n^2)<O(n^3)<...<O(2^n)<O(n!)
  
·大O表示法的运算规则
·最好情况、最坏情况、平均情况
·算法空间
  ·固定空间
    ·输入数据、结果数据、程序代码所需的空间
  ·可变空间（辅助空间）
    ·中间结果、递归调用所需的空间
·算法所需的辅助空间度量
  ·加入随着问题规模n的增长，算法运行所需辅助空间S(n)的增长率与g(n)的增长率相同，则记作：S(n)=O(g(n))，称O(g(n))为算法的空间复杂度
·算法原地工作:O(1)
//end

2.1 线性表的基本概念
·线性表特点
  ·唯一头尾
  ·前驱+后继
·线性表是类型相同的元素有限序列

·对线性表的ADT描述
  ·基本操作并不是硬性规定
  ·自己写，开源，收集bug
  函数指针？
